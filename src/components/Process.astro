---
const steps = [
  {
    number: '01',
    title: 'Discover',
    description: 'Understand the problem, research the landscape, identify opportunities.'
  },
  {
    number: '02',
    title: 'Plan',
    description: 'Define strategy, set priorities, map out the approach with clear milestones.'
  },
  {
    number: '03',
    title: 'Execute',
    description: 'Build, implement, and ship. Focus on what moves the needle.'
  },
  {
    number: '04',
    title: 'Iterate',
    description: 'Measure results, learn from data, refine and improve continuously.'
  }
];
---

<section class="py-20 lg:py-28 bg-cream-100">
  <div class="max-w-6xl mx-auto px-6 lg:px-8">
    <!-- Section Header -->
    <div class="max-w-2xl mb-14 animate-on-scroll">
      <p class="text-copper-600 font-semibold text-xs uppercase tracking-[0.2em] mb-3">Process</p>
      <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-slate-900 mb-4 leading-tight">
        How I approach work
      </h2>
      <p class="text-slate-800/70 leading-relaxed">
        A practical, iterative approach focused on real results rather than perfect plans.
      </p>
    </div>
    
    <!-- Process Steps -->
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
      {steps.map((step, index) => (
        <div 
          class="animate-on-scroll"
          style={`animation-delay: ${0.1 * (index + 1)}s;`}
        >
          <span class="text-4xl font-extrabold text-slate-900/15 mb-3 block">{step.number}</span>
          <h3 class="text-lg font-bold text-slate-900 mb-2">{step.title}</h3>
          <p class="text-slate-800/70 text-sm leading-relaxed">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
  
  <!-- Bottom border -->
  <div class="max-w-6xl mx-auto px-6 lg:px-8 mt-20">
    <div class="h-px bg-gradient-to-r from-transparent via-slate-900/15 to-transparent"></div>
  </div>
</section>
